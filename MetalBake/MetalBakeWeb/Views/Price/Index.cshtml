@model List<MetalBake.core.Models.ItemPrice>
@{
    ViewBag.Title = "Index";
}

<h2>Lista de precios</h2>

<table style="width:200px">
    <tr>
        <th>Producto</th>
        <th>Precio</th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.ItemId</td>
            <td>@item.Price</td>
        </tr>
    }
</table>